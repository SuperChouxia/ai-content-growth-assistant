# 内容获客定位助手 - AI/效率工具顾问提示词结构与人工审核规则

更新日期：2026-06-11

## 结论

AI/效率工具顾问方向的提示词不能设计成“一次性生成爆款文案”。

推荐设计成一条分步判断链：

**资料完整性检查 -> 定位诊断 -> 内容主线 -> 选题系统 -> 对标结构参考 -> 单篇内容草稿 -> 人工审核。**

核心原则：

**先判断，再生成；先业务场景，再工具；先真实案例，再表达。**

## 推荐方案

### 提示词不是万能入口

P0 不应该让用户直接输入：

> 帮我写一篇小红书爆款笔记。

而应该让系统先判断：

- 用户资料是否足够；
- 是否有明确服务对象；
- 是否有具体业务场景；
- 是否有 AI 工作流能力；
- 是否有真实案例或经验；
- 是否存在夸大、搬运、洗稿风险。

只有通过基础检查后，才进入定位、选题和内容生成。

## 提示词链路

### Step 1：资料完整性检查

目标：

判断用户资料是否足以生成定位诊断。

输入：

- 服务对象；
- 业务场景；
- AI/效率工具能力；
- 工作流能力；
- 真实案例；
- 转化路径；
- 内容禁区；
- 对标账号。

输出：

| 检查项 | 是否足够 | 缺失内容 | 补充问题 |
| --- | --- | --- | --- |
| 服务对象 | | | |
| 业务场景 | | | |
| AI 工作流能力 | | | |
| 真实案例 | | | |
| 转化路径 | | | |
| 内容边界 | | | |

提示词要求：

- 如果资料不足，优先输出补充问题；
- 不允许为了生成而编造案例；
- 不允许把“会用很多工具”当成完整定位；
- 不进入正文生成。

### Step 2：账号定位诊断

目标：

把用户从“工具分享者”定位成“某类业务场景的 AI/效率解决方案提供者”。

输入：

- Step 1 通过后的结构化资料。

输出：

- 账号一句话定位；
- 服务对象；
- 核心业务场景；
- 内容主张；
- 专业能力证明；
- 差异化；
- 转化路径；
- 不建议方向。

提示词约束：

- 必须包含服务对象和业务场景；
- 必须体现 AI 工作流能力；
- 必须引用真实案例或经验；
- 必须输出“不是……而是……”的内容主张；
- 不得输出“分享 AI 工具和效率方法”这类泛定位。

### Step 3：内容主线生成

目标：

把定位诊断转成 3-5 条可持续发布的内容主线。

输入：

- 账号定位；
- 核心业务场景；
- 用户痛点；
- AI 工作流能力；
- 案例素材；
- 转化路径。

输出：

| 主线 | 解决的用户问题 | 对应服务价值 | 可引用素材 | 适合内容类型 | 转化目标 |
| --- | --- | --- | --- | --- | --- |

提示词约束：

- 内容主线不能只围绕工具功能；
- 每条主线都必须连接服务价值；
- 每条主线都要能继续生成多个选题；
- 至少一条主线必须体现案例或前后对比。

### Step 4：选题系统生成

目标：

生成 30 个服务于定位的选题。

输入：

- 内容主线；
- 业务场景；
- 案例素材；
- 方法论；
- 对标结构参考；
- 转化目标。

输出：

| 序号 | 选题标题 | 类型 | 目标用户痛点 | 业务场景 | 可引用案例/经验 | 转化目标 | 风险边界 |
| --- | --- | --- | --- | --- | --- | --- | --- |

提示词约束：

- 小红书笔记标题默认不超过 20 个字；
- 每个选题必须绑定业务场景；
- 至少 50% 选题体现案例、判断或方法论；
- 不能输出纯工具清单；
- 不能搬运爆款标题；
- 不能承诺自动赚钱、必涨粉、必成交。

### Step 5：对标结构参考

目标：

拆解对标账号的结构，而不是复制对方表达。

输入：

- 对标账号名称或描述；
- 用户喜欢的点；
- 用户不想学的点；
- 当前用户定位；
- 当前用户业务场景。

输出：

| 对标账号 | 可借鉴点 | 不可照搬点 | 适合当前用户的结构 | 可转化选题 |
| --- | --- | --- | --- | --- |

提示词约束：

- 只拆结构；
- 不复制标题；
- 不复制正文；
- 所有建议必须回到用户自己的业务场景和案例；
- 必须写出不可照搬点。

### Step 6：单篇内容草稿生成

目标：

把一个选题生成可编辑的小红书笔记和短视频脚本。

输入：

- 选题；
- 账号定位；
- 业务场景；
- 真实案例或经验；
- 转化目标；
- 风险边界。

输出：

- 小红书标题；
- 封面文案；
- 正文结构；
- 评论引导；
- 私信引导；
- 短视频脚本。

提示词约束：

- 小红书笔记标题不超过 20 个字；
- 必须绑定具体业务场景；
- 必须体现用户自己的判断；
- 必须引用真实案例或经验；
- 不承诺爆款、成交或自动获客；
- 不复制对标账号表达；
- 输出必须保留人工编辑空间。

### Step 7：人工审核

目标：

在内容交付或产品输出前，检查是否符合账号定位、平台风险和用户真实能力。

输出：

| 审核项 | 结果 | 问题 | 修改建议 |
| --- | --- | --- | --- |
| 定位是否清楚 | | | |
| 是否绑定业务场景 | | | |
| 是否体现真实案例 | | | |
| 是否过度工具化 | | | |
| 是否有夸大承诺 | | | |
| 是否有搬运风险 | | | |
| 标题是否超过 20 字 | | | |

## 人工审核规则

### A 类：必须拦截

出现以下情况，不能交付，必须重写：

- 编造案例；
- 暴露客户隐私；
- 复制对标账号标题或正文；
- 承诺爆款、涨粉、成交；
- 承诺一键自动赚钱；
- 把内容写成工具清单；
- 没有业务场景；
- 小红书笔记标题超过 20 个字；
- 诱导自动私信、自动评论或自动发布。

### B 类：需要修改

出现以下情况，可以保留方向，但必须修改：

- 目标用户太泛；
- 服务价值不清楚；
- 案例不够具体；
- 转化引导太硬；
- 标题像教程但不像获客内容；
- 内容只有方法，没有判断；
- 对标拆解只写了“可以学习”，没有说明学什么。

### C 类：可以通过

满足以下标准，可以进入下一步：

- 有明确目标用户；
- 有明确业务场景；
- 有 AI 工作流能力；
- 有真实案例或经验；
- 有自然转化目标；
- 有不建议方向；
- 没有搬运、洗稿和夸大承诺。

## 审核顺序

人工审核按照这个顺序：

1. 先看定位是否成立；
2. 再看选题是否服务定位；
3. 再看内容是否绑定场景；
4. 再看有没有真实案例或经验；
5. 再看标题是否超过 20 字；
6. 最后看是否存在合规或平台风险。

不要先改文案细节，再回头发现定位不成立。

## 输出失败处理

### 资料不足

输出：

- 缺少哪些字段；
- 为什么这些字段重要；
- 需要用户补充的 3-5 个问题。

不输出：

- 账号定位；
- 30 个选题；
- 单篇内容草稿。

### 案例不足

可以输出：

- 初步定位；
- 可选内容主线；
- 需要补案例的提示。

不应输出：

- 强案例型笔记；
- 前后对比结果；
- 具体客户结果。

### 用户方向有风险

例如用户想做：

- 自动赚钱；
- 爆款搬运；
- 提示词合集号；
- AI 代运营承诺；
- 自动私信获客。

输出：

- 风险说明；
- 建议调整方向；
- 可替代表达。

## 后续用途

这份文档后续可以用于：

- 编写系统提示词；
- 设计页面字段；
- 制作人工审核清单；
- 生成交付模板；
- 设计后续产品测试标准。

当前不进入开发，也不展开首批用户执行。
