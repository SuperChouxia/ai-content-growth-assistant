# 内容获客定位助手 - AI/效率工具顾问 PRD 功能边界

更新日期：2026-06-11

## 结论

面向 AI/效率工具顾问的第一版产品，不应该做成“内容运营全平台”，而应该先做成：

**AI/效率工具顾问的内容获客定位诊断工作台。**

P0 只解决一个核心问题：

**帮用户从“我会很多工具”，收敛成“我能帮哪类人解决哪类工作问题，并用什么内容持续证明这件事”。**

## 推荐方案

### P0 边界

P0 只保留 7 个能力：

1. 业务场景资料采集；
2. AI 工作流能力采集；
3. 真实案例结构化；
4. 账号定位诊断；
5. 内容主线和选题系统；
6. 对标结构参考；
7. 单篇内容输出。

暂时不把发布计划、发布数据复盘、自动采集、自动发布、账号授权、支付订阅、团队协作放进 P0。

资料字段首屏压缩见：`23_AI效率工具顾问P0资料字段压缩方案.md`。

说明：

P0 要覆盖业务场景、AI 工作流和真实案例，但不代表用户首次进入时要一次性填写全部字段。首屏只保留 8 个必填判断字段，其余信息在定位、选题、对标和草稿阶段按需追问。

### 产品形态

当前仍然是产品草案和低保真设计前置阶段，不进入开发。

如果后续进入产品化，第一版更像一个轻量诊断工作台，而不是完整 SaaS。

## P0 功能定义

### 1. 业务场景资料采集

目标：

让用户先讲清楚自己服务谁、解决什么问题，而不是先罗列工具。

P0 需要覆盖：

- 角色类型；
- 目标用户或团队；
- 核心业务场景；
- 当前用户痛点；
- 服务结果；
- 转化方式；
- 不想变成的账号类型。

输出：

- 服务对象判断；
- 核心业务场景摘要；
- 内容定位的初始方向；
- 不建议方向。

验收标准：

- 能判断用户不是单纯工具推荐号；
- 能把“工具能力”转成“业务问题”；
- 能为定位诊断提供足够上下文。

### 2. AI 工作流能力采集

目标：

明确用户真正擅长的不是哪些工具，而是哪类工作流。

P0 需要覆盖：

- 常用 AI/效率工具；
- 擅长的工作流；
- 方法论；
- 哪些环节必须人工判断；
- 哪些环节可以自动化；
- 工具选择理由。

输出：

- 工作流能力摘要；
- 工具和业务场景的关系；
- 可内容化的方法论；
- 不能夸大的能力边界。

验收标准：

- 不把工具名当作核心卖点；
- 能看出用户的判断能力；
- 能提炼出可持续发布的工作流主题。

### 3. 真实案例结构化

目标：

把案例变成信任内容和选题素材。

P0 需要覆盖：

- 服务对象；
- 原始工作问题；
- 用户设计的 AI/效率流程；
- 使用工具；
- 前后变化；
- 可公开细节；
- 不可公开边界。

输出：

- 可公开案例摘要；
- 可引用证据；
- 前后对比；
- 内容风险提示；
- 可衍生选题。

验收标准：

- 不伪造案例；
- 不暴露客户隐私；
- 每个案例都能连接到一个业务场景；
- 每个案例都能转成至少 3 个选题方向。

### 4. 账号定位诊断

目标：

把用户从“AI 工具分享者”定位成“某类业务场景的 AI/效率解决方案提供者”。

必须输出：

- 账号一句话定位；
- 服务对象；
- 核心业务场景；
- 内容主张；
- 专业能力证明；
- 差异化；
- 转化路径；
- 不建议方向。

验收标准：

- 不能只写“分享 AI 工具和效率方法”；
- 必须说明服务对象和业务场景；
- 必须体现工作流能力和真实案例；
- 必须明确不建议做工具清单号、热点搬运号或提示词合集号。

### 5. 内容主线和选题系统

目标：

让用户知道接下来应该围绕什么长期发布。

必须输出：

- 3-5 条内容主线；
- 每条主线的目标用户痛点；
- 每条主线可承接的服务价值；
- 30 个选题；
- 每个选题的类型、业务场景、转化目标和风险边界。

选题类型：

- 场景型；
- 案例型；
- 工作流拆解型；
- 工具选择判断型；
- 避坑型；
- 方法论型；
- 服务转化型。

验收标准：

- 选题不能只围绕工具功能；
- 每个选题必须绑定业务场景；
- 至少 50% 选题要能体现案例、判断或方法论；
- 每个选题要有明确转化目标或信任目标。

### 6. 对标结构参考

目标：

帮助用户看懂同类账号为什么有效，但不照搬。

P0 需要覆盖：

- 对标账号；
- 用户喜欢它什么；
- 用户不想学它什么；
- 想学习的结构部分。

必须输出：

- 可借鉴点；
- 不可照搬点；
- 适合当前用户的结构；
- 不适合当前用户的表达；
- 可转化选题。

验收标准：

- 不做自动抓取；
- 不做爆款搬运；
- 不复制表达；
- 每个对标结论都要回到用户自己的业务场景和案例。

### 7. 单篇内容输出

目标：

把定位、选题和案例转成可编辑的内容草稿。

必须输出：

- 小红书标题；
- 封面文案；
- 正文；
- 评论引导；
- 私信引导；
- 短视频脚本。

生成约束：

- 必须绑定具体业务场景；
- 必须引用真实经验或案例；
- 不只介绍工具功能；
- 不复制对标表达；
- 不承诺夸大结果；
- 保留人工编辑空间。

验收标准：

- 内容不像工具清单；
- 内容能看出用户的判断；
- 内容能服务于咨询、诊断、课程、社群或工作流服务；
- 内容不承诺爆款、成交或自动获客。

## P1 功能

P1 可以在 P0 跑清楚后再考虑。

包括：

- 轻量发布计划；
- 发布后手动复盘；
- 选题优先级排序；
- 内容风格配置；
- 细分模板；
- 案例库；
- 标题多版本；
- 简单导出。

P1 的判断标准：

只有当它能帮助用户持续发布和复盘，才进入。

## P2 功能

P2 暂缓。

包括：

- 平台账号授权；
- 自动抓取对标账号数据；
- 自动发布；
- 自动评论；
- 自动私信；
- 多账号矩阵；
- 团队权限；
- CRM；
- 支付订阅；
- 手机 App；
- 复杂数据看板。

这些功能要么开发重，要么容易带来合规风险，要么会让产品过早平台化。

## 暂不做

### 不做一键爆款

原因：

- 会吸引错误用户；
- 与内容获客定位判断不一致；
- 容易滑向搬运和洗稿。

### 不做自动抓取和自动发布

原因：

- 平台风险高；
- 当前核心不确定性不是技术；
- 会让产品过早复杂化。

### 不做代运营

原因：

- 用户必须自己理解定位和内容判断；
- 项目目标是工具和判断辅助，不是替用户经营账号；
- 代运营会改变成本结构和交付边界。

### 不做复杂复盘

原因：

- 当前不展开首批用户执行；
- 复盘必须建立在真实发布数据上；
- 现阶段先保留为后续能力。

## 对当前 PRD 的调整建议

### 应保留

- 项目资料；
- 账号定位生成；
- 服务卖点与目标用户拆解；
- 选题生成；
- 对标账号拆解；
- 内容生成；
- 暂不做功能；
- AI 输出约束。

### 应降级

- 发布计划：从 P0 降为 P1；
- 发布复盘：从 P0 降为 P1；
- 数据结构草案：保留为未来参考，不作为当前开发依据；
- 开发优先级：改成未来产品化参考，当前不执行。

### 应补强

- 业务场景字段；
- AI 工作流能力字段；
- 方法论字段；
- 案例结构化字段；
- 不想变成的账号类型；
- 内容主线。

## 风险

### 风险 1：P0 仍然太重

如果 P0 同时做定位、选题、对标、内容、发布和复盘，会变成完整运营平台。

应对方式：

P0 先聚焦“定位诊断 + 选题系统 + 单篇内容输出”。

### 风险 2：内容生成抢走主价值

如果入口是“生成笔记”，用户会把产品理解成 AI 写作工具。

应对方式：

产品流程必须先做业务场景、工作流和案例，再生成内容。

### 风险 3：用户案例不足

没有真实案例，就很难生成可信内容。

应对方式：

未来进入验证阶段时，把真实案例作为进入门槛之一。

当前阶段不做用户筛选。

## 建议动作

1. P0 输出模板已经收敛为定位诊断、选题系统和单篇内容草稿；
2. 后续提示词、页面结构和人工交付模板都应引用这三类输出；
3. 暂不进入设计和开发；
4. 暂不展开首批用户执行。
