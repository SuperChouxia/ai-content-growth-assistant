# 内容获客定位助手 - AI/效率工具顾问 P0 资料字段压缩方案

更新日期：2026-06-11

## 结论

P0 业务资料页不应该一次性展示完整调研表。

推荐改成两层：

1. **必填判断字段**：先问 8 个问题，用来判断能不能生成定位；
2. **补充字段**：在定位、选题和草稿阶段按需追问。

压缩后的目标不是少收资料，而是降低用户第一次进入产品的填写压力。

## 推荐方案

### P0 首屏只问 8 个必填判断字段

| 序号 | 字段 | 用户要回答什么 | 用途 |
| --- | --- | --- | --- |
| 1 | 当前角色 | 你现在更接近哪类 AI/效率顾问 | 判断账号基本类型 |
| 2 | 服务对象 | 你最想帮哪类人或团队 | 判断目标用户是否具体 |
| 3 | 核心业务场景 | 他们在哪类工作里最卡 | 判断内容是否有场景 |
| 4 | 服务或产品 | 你最终承接什么服务、产品或咨询 | 判断转化路径 |
| 5 | 可感知结果 | 用户通过你能获得什么变化 | 生成账号定位里的结果表达 |
| 6 | 主要 AI 工作流 | 你最擅长把哪类工作拆成流程 | 判断不是纯工具推荐 |
| 7 | 真实案例或经验 | 你有没有一个可公开的案例或亲身经验 | 判断可信度和选题素材 |
| 8 | 内容边界 | 你不想变成什么账号、不能承诺什么、不能公开什么 | 控制风险 |

### 为什么是这 8 个

这 8 个字段刚好支撑 P0 的三个核心输出：

| P0 输出 | 必须依赖的字段 |
| --- | --- |
| 账号定位诊断 | 当前角色、服务对象、业务场景、服务结果、转化路径 |
| 内容主线和选题系统 | 业务场景、AI 工作流、案例或经验、内容边界 |
| 单篇内容草稿 | 选题、案例或经验、服务结果、风险边界 |

如果这 8 个字段不完整，系统不应该直接生成正文。

## 字段压缩前后对比

### 原完整字段

原资料采集包含 21 个问题，覆盖：

- 角色；
- 服务对象；
- 业务场景；
- 产品服务；
- 价格；
- 工具；
- 工作流；
- 方法论；
- 真实案例；
- 失败案例；
- 用户痛点；
- 内容现状；
- 对标账号；
- 转化路径；
- 风险边界。

这些字段都重要，但不适合全部放在 P0 首屏。

### 压缩后字段分层

| 层级 | 字段数量 | 作用 | 展示时机 |
| --- | --- | --- | --- |
| 必填判断字段 | 8 个 | 判断能不能生成定位 | 首次填写 |
| 补充字段 | 10-15 个 | 提高选题和内容质量 | 按页面追问 |
| 后续验证字段 | 暂不进入 P0 | 支持发布、复盘和服务交付 | P1/P2 |

## 必填判断字段详情

### 1. 当前角色

问题：

> 你现在更接近哪类 AI/效率顾问？

选项：

- AI 工具顾问；
- AI 工作流顾问；
- 自动化流程顾问；
- 效率工具培训者；
- 企业 AI 应用教练；
- AI 课程/训练营老师；
- 小团队 AI 落地顾问；
- 其他。

判断逻辑：

- 如果选择工具顾问，需要后续追问业务场景；
- 如果选择工作流顾问，可以优先进入流程能力判断；
- 如果选择课程/训练营，需要后续关注转化路径。

### 2. 服务对象

问题：

> 你最想帮哪类人或团队？

填写要求：

- 不能写“所有想学 AI 的人”；
- 尽量具体到职业、团队类型或业务状态；
- 允许先写一个主对象，不要求覆盖全部。

示例：

- 一人公司；
- 知识服务团队；
- 咨询型个人 IP；
- 小红书运营团队；
- 企业培训负责人。

判断逻辑：

- 如果服务对象太泛，状态为 `资料不足`；
- 如果服务对象具体，可以继续判断业务场景。

### 3. 核心业务场景

问题：

> 他们在哪类工作里最卡？

示例：

- 选题和脚本产出慢；
- 内容素材散乱；
- 客户资料整理耗时；
- 团队不知道怎么开始用 AI；
- 重复运营动作太多；
- 知识库搭建困难。

判断逻辑：

- 如果只写“提升效率”，要求补充具体工作；
- 如果能说清楚工作环节，可以进入定位诊断。

### 4. 服务或产品

问题：

> 你最终承接什么服务、产品或咨询？

选项：

- 一对一咨询；
- 工作流诊断；
- 工作流搭建服务；
- 企业内训；
- 课程；
- 训练营；
- 社群；
- 模板/资料包；
- 陪跑服务；
- 其他。

判断逻辑：

- 用于判断内容的转化目标；
- 如果没有任何承接产品，不能进入完整定位。

### 5. 可感知结果

问题：

> 用户通过你，能获得什么可感知变化？

填写要求：

- 不写“学会 AI”；
- 不承诺必然结果；
- 要写成用户能理解的业务变化。

示例：

- 把选题、脚本和素材整理成可复用流程；
- 搭建内容资料库，减少反复找素材；
- 让小团队知道哪些工作适合用 AI，哪些必须人工判断；
- 把零散工具用法整理成一套工作流。

判断逻辑：

- 用于生成一句话定位；
- 如果结果太夸大，进入风险修改。

### 6. 主要 AI 工作流

问题：

> 你最擅长把哪类工作拆成流程？

示例：

- 内容选题到脚本；
- 资料库搭建；
- 客户资料整理；
- 课程研发；
- 小红书内容生产；
- 视频脚本生产；
- 自动化任务流。

判断逻辑：

- 如果只列工具名，状态为 `资料不足`；
- 如果能说明流程，说明用户具备工作流表达基础。

### 7. 真实案例或经验

问题：

> 你有没有一个可公开的案例或亲身经验？

最小填写结构：

- 服务对象是谁；
- 原来卡在哪里；
- 你怎么拆流程；
- 哪些变化可以公开讲；
- 哪些信息不能公开。

判断逻辑：

- 有案例：可以生成完整定位和案例型选题；
- 没案例但有亲身经验：可以生成初步定位和方法论型选题；
- 没案例也没经验：不生成强内容，只输出补充问题。

### 8. 内容边界

问题：

> 哪些方向你不想做、不能承诺、不能公开？

拆成三个小问题：

| 小问题 | 示例 |
| --- | --- |
| 不想变成什么账号 | 工具清单号、提示词合集号、热点搬运号 |
| 不能承诺什么 | 爆款、涨粉、成交、自动赚钱、自动获客 |
| 不能公开什么 | 客户姓名、收入数据、聊天记录、内部资料 |

判断逻辑：

- 用于人工审核；
- 用于生成不建议方向；
- 用于拦截搬运、洗稿和夸大承诺。

## 补充字段

补充字段不在首屏强制填写，而是在具体页面按需追问。

### 定位诊断页补充字段

| 字段 | 追问时机 | 用途 |
| --- | --- | --- |
| 价格区间 | 转化路径不清楚时 | 判断内容信任门槛 |
| 当前账号状态 | 定位偏差不清楚时 | 判断从哪里调整 |
| 为什么不只看免费教程 | 专业价值不清楚时 | 提炼差异化 |

### 选题页补充字段

| 字段 | 追问时机 | 用途 |
| --- | --- | --- |
| 目标用户常问问题 | 选题不够具体时 | 生成痛点型选题 |
| 失败或踩坑经验 | 内容太像教程时 | 生成避坑型选题 |
| 方法论 | 选题缺少判断时 | 生成方法论型选题 |

### 对标页补充字段

| 字段 | 追问时机 | 用途 |
| --- | --- | --- |
| 对标账号 | 用户需要参考时 | 做结构拆解 |
| 喜欢它什么 | 拆解结构时 | 提炼可借鉴点 |
| 不想学它什么 | 防止照搬时 | 输出不可照搬点 |

### 草稿页补充字段

| 字段 | 追问时机 | 用途 |
| --- | --- | --- |
| 选题目标 | 生成单篇草稿前 | 判断开头和结尾 |
| 可引用素材 | 案例不足时 | 避免编造 |
| 引导动作 | 需要转化时 | 生成自然评论/私信引导 |

## 页面状态调整

### 新增状态：可初步定位

触发条件：

- 服务对象明确；
- 业务场景明确；
- AI 工作流明确；
- 但案例不足。

允许：

- 输出初步定位；
- 输出内容主线；
- 输出方法论型、避坑型选题。

不允许：

- 输出强案例型内容；
- 输出前后对比；
- 暗示用户已有客户结果。

### 新增状态：可完整生成

触发条件：

- 8 个必填判断字段全部通过；
- 至少有一个可公开案例或真实经验；
- 内容边界清楚。

允许：

- 输出定位诊断；
- 输出 30 个选题；
- 输出单篇内容草稿；
- 进入人工审核。

### 保留状态：资料不足

触发条件：

- 服务对象不清；
- 业务场景不清；
- 只有工具名；
- 没有服务或产品；
- 内容边界缺失。

处理：

- 不生成定位；
- 输出补充问题；
- 引导回业务资料页。

## 对页面结构的影响

### 业务资料页调整

从 4 个大区块改为 3 个层级：

1. 必填判断字段；
2. 补充信息；
3. 风险边界。

页面默认只展开必填判断字段。

补充信息可以折叠，等系统需要时再提示填写。

### 项目首页调整

首页增加：

- 当前最小可生成能力；
- 缺失字段数量；
- 下一步必须补什么；
- 是否能生成初步定位。

### 定位诊断页调整

定位页要显示：

- 本次定位基于哪些字段；
- 哪些字段缺失；
- 是初步定位还是完整定位；
- 是否允许进入选题。

## 字段验收标准

### 通过标准

- 首次填写不超过 8 个核心问题；
- 用户能在 5-8 分钟内完成初填；
- 系统能判断是 `资料不足`、`可初步定位` 还是 `可完整生成`；
- 必填字段能支撑账号定位；
- 补充字段不阻塞首次进入定位诊断；
- 风险边界在生成内容前必须确认。

### 不通过标准

- 首屏字段超过 10 个；
- 用户必须一次性填写对标账号；
- 用户必须一次性填写多个案例；
- 用户必须先填写价格和发布计划；
- 没有案例却生成案例型内容；
- 没有边界却进入草稿生成。

## 对当前文档的影响

需要同步调整：

- `15_AI效率工具顾问产品入口与资料采集字段.md`
- `19_AI效率工具顾问P0页面结构草案.md`
- `20_AI效率工具顾问P0用户路径与状态流转.md`
- `21_AI效率工具顾问P0结构型低保真原型.md`
- `22_AI效率工具顾问P0模拟用户样例与路径跑通.md`

## 下一步

1. 基于压缩后的字段准备可视化低保真线框；
2. 线框只验证页面布局、状态和字段层级，不做高保真视觉；
3. 当前仍不进入开发，也不展开首批用户执行。
