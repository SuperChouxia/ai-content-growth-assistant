# 内容获客定位助手 - AI/效率工具顾问 P0 页面结构草案

更新日期：2026-06-11

## 结论

P0 页面结构不做完整运营平台，只做一个轻量诊断工作台。

页面目标是帮助 AI/效率工具顾问完成：

**资料录入 -> 定位诊断 -> 内容主线 -> 选题系统 -> 对标结构 -> 单篇内容草稿 -> 人工审核。**

用户路径和状态流转见：`20_AI效率工具顾问P0用户路径与状态流转.md`。

结构型低保真原型见：`21_AI效率工具顾问P0结构型低保真原型.md`。

当前只是产品页面结构草案，不做视觉设计、不做原型、不进入开发。

## 页面总览

P0 建议保留 6 个核心页面：

1. 项目首页；
2. 业务资料；
3. 定位诊断；
4. 内容主线和选题库；
5. 对标结构参考；
6. 单篇内容草稿。

P1 再考虑：

- 发布计划；
- 发布复盘；
- 案例库；
- 标题多版本；
- 导出中心。

## 页面 1：项目首页

### 目标

让用户一眼看到当前项目是否已经具备生成定位和选题的基础。

### 核心模块

- 项目名称；
- 当前账号定位摘要；
- 资料完整度；
- P0 输出状态；
- 下一步建议；
- 风险提醒。

### 关键状态

| 状态 | 含义 | 下一步 |
| --- | --- | --- |
| 资料不足 | 缺少服务对象、业务场景或案例 | 补充资料 |
| 可生成定位 | 核心资料已够 | 进入定位诊断 |
| 可生成选题 | 定位已确认 | 进入选题系统 |
| 可生成内容 | 已选择选题 | 进入单篇内容草稿 |

### 不放什么

- 不放复杂数据看板；
- 不放发布增长曲线；
- 不放自动运营按钮；
- 不放营销式欢迎页。

## 页面 2：业务资料

### 目标

收集足够的业务场景、AI 工作流和真实案例，避免后续输出泛化。

### 核心模块

#### 1. 基础定位

- 角色类型；
- 服务对象；
- 核心业务场景；
- 服务结果；
- 转化方式。

#### 2. AI 工作流能力

- 常用工具；
- 擅长工作流；
- 方法论；
- 哪些环节必须人工判断；
- 哪些环节可以自动化。

#### 3. 真实案例

- 服务对象；
- 原始问题；
- 设计的流程；
- 使用工具；
- 前后变化；
- 可公开内容；
- 不可公开边界。

#### 4. 内容边界

- 不想变成什么账号；
- 不能承诺什么；
- 不能公开什么；
- 不想学习哪些对标表达。

### 页面规则

- 必填字段少而关键；
- 补充字段可以后填；
- 资料不足时不硬生成；
- 每个字段都要说明为什么要问。

## 页面 3：定位诊断

### 目标

把用户从“AI 工具分享者”定位成“某类业务场景的 AI/效率解决方案提供者”。

### 核心模块

- 账号一句话定位；
- 目标用户判断；
- 核心业务场景；
- 专业能力证明；
- 内容主张；
- 差异化；
- 转化路径；
- 不建议方向。

### 交互逻辑

用户可以：

- 编辑定位；
- 标记不准确；
- 返回补充资料；
- 确认后进入内容主线。

### 页面规则

- 定位不成立时，不进入内容生成；
- 必须显示“不建议方向”；
- 必须提醒用户不要做工具清单号、热点搬运号或提示词合集号。

## 页面 4：内容主线和选题库

### 目标

把定位诊断转成可持续发布的内容系统。

### 核心模块

#### 1. 内容主线

每条主线显示：

- 主线名称；
- 解决的用户问题；
- 对应服务价值；
- 可引用素材；
- 适合内容类型；
- 转化目标。

#### 2. 选题库

每个选题显示：

- 选题标题；
- 类型；
- 目标用户痛点；
- 业务场景；
- 可引用案例或经验；
- 转化目标；
- 风险边界。

### 筛选方式

- 按内容主线；
- 按选题类型；
- 按业务场景；
- 按转化目标；
- 按风险边界。

### 页面规则

- 小红书笔记标题默认不超过 20 个字；
- 选题不能只围绕工具功能；
- 每个选题必须绑定业务场景；
- 至少 50% 选题体现案例、判断或方法论。

## 页面 5：对标结构参考

### 目标

帮助用户从对标账号中学习结构，而不是复制内容。

### 核心模块

- 对标账号；
- 用户喜欢它什么；
- 用户不想学它什么；
- 可借鉴点；
- 不可照搬点；
- 适合当前用户的结构；
- 可转化选题。

### 页面规则

- 不做自动抓取；
- 不复制标题和正文；
- 必须输出不可照搬点；
- 所有建议必须回到用户自己的业务场景。

## 页面 6：单篇内容草稿

### 目标

把一个选题生成可编辑的小红书笔记和短视频脚本。

### 核心模块

#### 1. 选题信息

- 选题标题；
- 所属内容主线；
- 业务场景；
- 转化目标；
- 风险边界。

#### 2. 小红书笔记

- 标题；
- 封面主标题；
- 封面副标题；
- 开头；
- 正文结构；
- 评论引导；
- 私信引导。

#### 3. 短视频脚本

- 开场钩子；
- 场景描述；
- 常见误区；
- 工作流拆解；
- 工具出现位置；
- 人工判断位置；
- 案例或经验；
- 结尾引导。

#### 4. 人工审核

- 标题是否超过 20 个字；
- 是否绑定业务场景；
- 是否引用真实案例或经验；
- 是否过度工具化；
- 是否有夸大承诺；
- 是否有搬运或洗稿风险；
- 是否暴露客户隐私。

### 页面规则

- 草稿必须可编辑；
- 不允许直接复制对标表达；
- 不允许承诺爆款、成交或自动获客；
- 如果审核不通过，需要回到定位、选题或资料页修改。

## P1 页面暂缓

### 发布计划

暂缓原因：

- 当前不展开首批用户执行；
- 发布计划依赖真实发布节奏；
- P0 先保证定位和内容质量。

### 发布复盘

暂缓原因：

- 复盘必须建立在真实数据上；
- 当前阶段只做产品文档；
- 不把数据看板提前做重。

### 导出中心

暂缓原因：

- 后续可以做 Markdown 或文档导出；
- 当前先定义输出结构。

## 页面顺序

推荐顺序：

1. 项目首页；
2. 业务资料；
3. 定位诊断；
4. 内容主线和选题库；
5. 对标结构参考；
6. 单篇内容草稿。

不推荐顺序：

1. 先输入爆款链接；
2. 先生成笔记；
3. 再补定位；
4. 最后再找案例。

原因：

这会让产品变成 AI 写作工具，而不是内容获客定位助手。

## 页面验收标准

### 信息架构通过标准

- 页面顺序符合“先判断，再生成”；
- 每个页面都有明确输入和输出；
- 每个页面都能对应到 P0 状态流转；
- P0 页面不包含自动发布、自动私信、复杂数据看板；
- 用户可以从页面结构理解产品不是代运营。

### 内容边界通过标准

- 所有内容生成前必须经过定位诊断；
- 所有选题必须绑定业务场景；
- 所有对标都必须有不可照搬点；
- 所有草稿都必须有人工审核。

## 建议动作

1. 后续如果进入设计阶段，再基于这份页面结构做低保真原型；
2. 当前不做视觉设计；
3. 当前不做前端开发；
4. 当前不展开首批用户执行。
