# 内容获客定位助手 - AI/效率工具顾问 P0 用户路径与状态流转

更新日期：2026-06-11

## 结论

P0 用户路径不应该从“生成一篇内容”开始，而应该从“判断资料是否足够支撑账号定位”开始。

推荐主路径：

**创建项目 -> 补充业务资料 -> 资料完整性检查 -> 定位诊断 -> 确认定位 -> 生成内容主线和选题 -> 参考对标结构 -> 生成单篇内容草稿 -> 人工审核。**

这份文档用于承接：

- `16_AI效率工具顾问PRD功能边界.md`
- `17_AI效率工具顾问P0输出模板.md`
- `18_AI效率工具顾问提示词结构与人工审核规则.md`
- `19_AI效率工具顾问P0页面结构草案.md`
- `21_AI效率工具顾问P0结构型低保真原型.md`
- `23_AI效率工具顾问P0资料字段压缩方案.md`

当前仍然是产品和设计前置文档，不进入视觉设计，不进入开发，也不展开首批用户执行。

## 推荐方案

### P0 只保留一条主闭环

P0 的主闭环是：

1. 判断用户是谁；
2. 判断用户能帮谁；
3. 判断用户解决什么业务问题；
4. 判断用户凭什么可信；
5. 把判断转成内容主线；
6. 把内容主线转成选题；
7. 把选题转成可编辑内容；
8. 由人工审核后再交付或发布。

不把发布计划、发布复盘、数据看板、自动发布和自动私信放入 P0。

### 用户路径原则

- 先补资料，再生成定位；
- 先确认定位，再生成选题；
- 先选择选题，再生成内容；
- 先人工审核，再进入发布或交付；
- 对标账号只做结构参考，不做内容复制；
- 任意环节资料不足，都允许退回补充资料。

## 主路径

### Step 1：创建项目

用户动作：

- 输入项目名；
- 选择角色类型；
- 选择首发平台；
- 填写当前一句话描述。

系统判断：

- 是否是 AI/效率工具顾问方向；
- 是否有明确服务或产品；
- 是否明显属于泛流量、搬运或工具清单需求。

输出状态：

- `资料待补充`
- `方向可能不匹配`
- `可进入业务资料页`

失败处理：

- 如果用户只想做爆款搬运，提示该产品不支持；
- 如果用户没有产品、服务或专业能力，提示先补充转化路径；
- 如果用户只是想生成一篇文案，提示需要先完成定位诊断。

### Step 2：补充业务资料

用户动作：

- 填写 8 个必填判断字段；
- 包括当前角色、服务对象、核心业务场景、服务或产品、可感知结果、主要 AI 工作流、真实案例或经验、内容边界；
- 按需补充工具、方法论、对标账号等非首屏字段。

系统判断：

- 服务对象是否具体；
- 业务场景是否清楚；
- 工作流能力是否能解释；
- 是否有真实案例或经验；
- 是否有明显夸大、隐私或合规风险。

输出状态：

- `资料不足`
- `可初步定位`
- `可完整生成`
- `存在风险需修改`

失败处理：

- 资料不足时，只输出补充问题；
- 案例不足但有真实经验时，可以进入初步定位，但不能生成强案例型内容；
- 存在隐私风险时，要求先修改可公开边界。

### Step 3：资料完整性检查

系统动作：

根据用户资料生成完整性检查表。

必须检查：

| 检查项 | 通过标准 | 不通过处理 |
| --- | --- | --- |
| 服务对象 | 能说清楚是哪类人或团队 | 要求缩小人群 |
| 业务场景 | 能说清楚具体工作问题 | 要求补充场景 |
| AI 工作流 | 能解释流程而不是只列工具 | 要求补充流程 |
| 真实案例 | 有可公开经验或案例 | 标记案例不足 |
| 转化路径 | 有自然承接产品或服务 | 要求补充服务 |
| 内容边界 | 知道不能说什么 | 要求补充禁区 |

输出状态：

- `可初步定位`
- `可完整生成`
- `需要补充资料`
- `暂不适合生成`

页面结果：

- 可初步定位时进入定位诊断，但标记案例不足；
- 可完整生成时进入定位诊断；
- 不通过则回到业务资料页；
- 暂不适合生成时，只给调整建议，不生成选题和正文。

### Step 4：定位诊断

系统动作：

生成账号定位诊断。

输出内容：

- 账号一句话定位；
- 目标用户判断；
- 核心业务场景；
- 内容主张；
- 专业能力证明；
- 差异化；
- 转化路径；
- 不建议方向。

用户动作：

- 确认定位；
- 修改定位；
- 标记不准确；
- 返回补充资料。

输出状态：

- `定位待确认`
- `定位已确认`
- `定位不成立`
- `需要补充资料`

失败处理：

- 如果定位仍然是“分享 AI 工具”，要求重写；
- 如果目标用户过宽，要求缩小；
- 如果没有业务场景，退回业务资料页；
- 如果没有真实能力证明，只允许进入基础内容主线，不生成案例型内容。

### Step 5：生成内容主线和选题

前置条件：

- 定位已确认。

系统动作：

- 生成 3-5 条内容主线；
- 每条主线绑定用户问题、服务价值、案例素材和转化目标；
- 生成 30 个选题；
- 每个选题绑定业务场景、内容类型和风险边界。

用户动作：

- 收藏选题；
- 删除不适合的选题；
- 标记选题风险；
- 选择一个选题进入内容草稿。

输出状态：

- `选题已生成`
- `选题需修改`
- `可生成内容草稿`

失败处理：

- 如果选题像工具清单，重新生成；
- 如果标题超过 20 个字，必须修改；
- 如果选题没有业务场景，不能进入内容生成；
- 如果选题承诺爆款、涨粉或成交，必须拦截。

### Step 6：对标结构参考

前置条件：

- 已有定位；
- 已有至少一条内容主线或选题。

用户动作：

- 添加对标账号或对标内容描述；
- 标记喜欢的结构；
- 标记不想学习的表达；
- 说明自己想借鉴的部分。

系统动作：

- 拆解可借鉴结构；
- 输出不可照搬点；
- 转成适合当前用户的内容结构；
- 生成可转化选题建议。

输出状态：

- `对标可参考`
- `对标不适合`
- `存在搬运风险`

失败处理：

- 不复制标题；
- 不复制正文；
- 不输出洗稿版本；
- 如果用户只想搬运，提示产品不支持。

### Step 7：生成单篇内容草稿

前置条件：

- 定位已确认；
- 已选择一个合格选题；
- 选题有业务场景和转化目标。

系统动作：

生成：

- 小红书标题；
- 封面文案；
- 正文结构；
- 评论引导；
- 私信引导；
- 短视频脚本。

输出状态：

- `草稿已生成`
- `草稿需修改`
- `进入人工审核`

失败处理：

- 标题超过 20 个字，必须重写；
- 没有真实案例或经验，不能写成案例复盘；
- 引导过硬，改成自然承接；
- 出现夸大承诺，必须删除；
- 出现隐私内容，必须脱敏。

### Step 8：人工审核

人工审核目标：

确保最终输出不是泛内容、不是搬运内容、不是夸大承诺，也不会伤害用户长期账号定位。

审核顺序：

1. 定位是否成立；
2. 选题是否服务定位；
3. 内容是否绑定业务场景；
4. 是否引用真实案例或经验；
5. 标题是否超过 20 个字；
6. 是否有搬运、洗稿、隐私或夸大风险；
7. 是否保留人工编辑空间。

输出状态：

- `审核通过`
- `需要小修`
- `需要重写`
- `退回定位`
- `退回资料`

通过后结果：

- 可以进入人工交付；
- 可以作为后续低保真原型的结果页样例；
- 可以作为未来开发验收样例。

当前不进入自动发布或自动私信。

## 状态流转

### 核心状态表

| 状态 | 触发条件 | 下一步 | 不能做什么 |
| --- | --- | --- | --- |
| 资料待补充 | 项目刚创建 | 补充业务资料 | 不能生成正文 |
| 资料不足 | 缺少服务对象、场景、工作流、服务或边界 | 补资料 | 不能生成定位 |
| 可初步定位 | 服务对象、场景和工作流清楚，但案例不足 | 生成初步定位 | 不能生成强案例内容 |
| 可完整生成 | 8 个必填判断字段通过 | 生成定位诊断 | 不能跳到正文 |
| 定位待确认 | 已生成定位 | 用户确认或修改 | 不能生成最终草稿 |
| 定位已确认 | 用户确认定位 | 生成内容主线和选题 | 不能修改为泛定位 |
| 选题已生成 | 已生成选题库 | 选择选题 | 不能默认发布 |
| 可生成内容草稿 | 选题合格 | 生成草稿 | 不能跳过审核 |
| 草稿已生成 | 已生成单篇内容 | 人工审核 | 不能自动发布 |
| 审核通过 | 风险检查通过 | 人工交付或后续发布 | 不能承诺结果 |
| 需要重写 | 出现 A 类风险 | 回到对应步骤 | 不能交付 |

### 页面与状态映射

| 页面 | 主要状态 | 关键动作 |
| --- | --- | --- |
| 项目首页 | 资料待补充、可初步定位、可完整生成、定位已确认 | 查看进度和下一步 |
| 业务资料 | 资料不足、可初步定位、可完整生成 | 补充和修改资料 |
| 定位诊断 | 定位待确认、定位已确认、定位不成立 | 确认或退回修改 |
| 内容主线和选题库 | 选题已生成、选题需修改、可生成内容草稿 | 选择和筛选选题 |
| 对标结构参考 | 对标可参考、对标不适合、存在搬运风险 | 只学习结构 |
| 单篇内容草稿 | 草稿已生成、草稿需修改、审核通过 | 编辑和审核内容 |

## 关键异常流程

### 异常 1：用户资料太泛

表现：

- 目标用户写“所有想学 AI 的人”；
- 业务场景写“提升效率”；
- 能力说明只列工具名。

处理：

- 不生成定位；
- 输出 3-5 个补充问题；
- 引导用户缩小到具体人群和场景。

### 异常 2：用户没有真实案例

表现：

- 没有服务过客户；
- 没有可公开结果；
- 只有工具体验。

处理：

- 可以生成初步定位；
- 可以生成方法论型和避坑型选题；
- 不生成强案例型内容；
- 提示后续需要补充真实经验。

### 异常 3：用户想学习爆款表达

表现：

- 只提供爆款标题；
- 要求改写成自己的版本；
- 希望保留对方表达。

处理：

- 只拆结构；
- 不复制标题；
- 不输出洗稿稿件；
- 要求回到用户自己的业务场景和案例。

### 异常 4：内容过度工具化

表现：

- 主要篇幅介绍工具功能；
- 看不出服务对象；
- 看不出业务问题；
- 看不出用户自己的判断。

处理：

- 退回内容主线；
- 补充业务场景；
- 把工具改成工作流里的一个环节。

### 异常 5：出现夸大或平台风险

表现：

- 承诺必爆、必涨粉、必成交；
- 承诺自动赚钱；
- 引导自动私信、自动评论、自动发布；
- 暴露客户隐私。

处理：

- 必须拦截；
- 删除风险表达；
- 改成边界清楚的经验、判断或方法论。

## 低保真原型准备判断

可以进入低保真原型的条件：

- P0 页面已经明确；
- 每个页面的输入和输出已经明确；
- 主路径状态已经明确；
- 异常流程已经明确；
- 人工审核规则已经明确；
- P0 不包含发布、复盘、自动抓取、自动私信等复杂能力。

当前判断：

**已经具备进入结构型低保真原型的基础，但还不建议进入视觉设计和开发。**

下一步如果继续推进设计，应先做：

- 页面线框；
- 页面字段布局；
- 主要状态样例；
- 空状态和失败状态；
- 一份完整模拟用户样例。

## 验收标准

### 产品路径通过标准

- 用户不能跳过资料检查直接生成正文；
- 用户不能跳过定位确认直接生成选题；
- 用户不能跳过人工审核直接进入发布；
- 任意生成结果都能追溯到业务资料、定位或选题；
- 对标账号只能影响结构，不能复制表达。

### 内容质量通过标准

- 标题不超过 20 个字；
- 内容绑定具体业务场景；
- 能看出 AI 工作流能力；
- 能看出用户自己的判断；
- 没有搬运、洗稿和夸大承诺；
- 没有隐私泄露。

### 阶段边界通过标准

- 不展开首批用户执行；
- 不做自动发布；
- 不做自动私信；
- 不做复杂数据看板；
- 不进入开发；
- 不把 P1/P2 能力塞进 P0。

## 建议动作

1. 用这份状态流转文档，检查 `19_AI效率工具顾问P0页面结构草案.md` 是否需要补页面状态；
2. 用 `21_AI效率工具顾问P0结构型低保真原型.md` 检查页面字段、状态和跳转；
3. 后续如进入开发阶段，再把状态表改成产品验收清单；
4. 当前仍不展开首批用户招募、交付和复盘执行。
