# 内容获客定位助手 - AI/效率工具顾问 P0 结构型低保真原型

更新日期：2026-06-11

## 结论

P0 低保真原型不做视觉风格，也不做可运行产品。

当前只定义一件事：

**用户在轻量诊断工作台里，如何一步步从业务资料，走到定位诊断、选题系统、对标结构参考、单篇内容草稿和人工审核。**

这份文档是设计前置产物，用于承接：

- `19_AI效率工具顾问P0页面结构草案.md`
- `20_AI效率工具顾问P0用户路径与状态流转.md`
- `22_AI效率工具顾问P0模拟用户样例与路径跑通.md`
- `23_AI效率工具顾问P0资料字段压缩方案.md`

当前仍不进入开发，不做视觉设计，不展开首批用户执行。

## 设计 Brief

### 产品对象

AI/效率工具顾问的内容获客定位诊断工作台。

### 用户目标

用户希望把自己的 AI 工具能力、工作流经验、服务对象和真实案例，整理成：

- 账号定位；
- 内容主线；
- 选题库；
- 对标结构参考；
- 单篇小红书笔记和短视频脚本。

### 产品目标

让用户不要直接进入“写一篇爆款文案”，而是先完成定位判断。

### 原型层级

结构型低保真原型。

只定义：

- 页面顺序；
- 页面区块；
- 字段；
- 操作；
- 状态；
- 跳转；
- 空状态；
- 失败状态；
- 验收标准。

不定义：

- 颜色；
- 字体；
- 品牌视觉；
- 动效；
- 真实前端组件；
- 代码结构；
- 服务端逻辑。

## 全局信息架构

### P0 页面

1. 项目首页；
2. 业务资料；
3. 定位诊断；
4. 内容主线和选题库；
5. 对标结构参考；
6. 单篇内容草稿。

### 全局导航

导航建议按任务顺序排列：

| 导航项 | 对应页面 | 是否 P0 | 进入条件 |
| --- | --- | --- | --- |
| 首页 | 项目首页 | 是 | 创建项目后可进入 |
| 资料 | 业务资料 | 是 | 创建项目后可进入 |
| 定位 | 定位诊断 | 是 | 资料检查通过 |
| 选题 | 内容主线和选题库 | 是 | 定位已确认 |
| 对标 | 对标结构参考 | 是 | 已有定位或选题 |
| 草稿 | 单篇内容草稿 | 是 | 已选择合格选题 |

暂不出现在 P0 导航：

- 发布计划；
- 发布复盘；
- 数据看板；
- 自动发布；
- 自动私信；
- 支付订阅；
- 团队协作。

### 全局顶部区

每个页面顶部都应显示：

- 项目名称；
- 当前状态；
- 当前定位摘要；
- 下一步建议；
- 风险提示入口。

示例字段：

| 字段 | 示例 | 作用 |
| --- | --- | --- |
| 项目名 | 一人公司 AI 工作流顾问 | 让用户知道当前编辑对象 |
| 当前状态 | 定位待确认 | 告诉用户下一步是什么 |
| 定位摘要 | 帮一人公司搭建内容工作流 | 防止页面操作脱离定位 |
| 下一步 | 确认定位后生成选题 | 降低用户迷路概率 |
| 风险提示 | 2 条待处理 | 提醒人工审核风险 |

### 全局底部操作

所有页面底部保留轻量操作：

- 保存；
- 返回上一步；
- 继续下一步；
- 查看审核问题。

操作规则：

- 资料不足时，下一步不可进入定位；
- 定位未确认时，下一步不可进入选题；
- 选题不合格时，下一步不可进入草稿；
- 草稿未审核时，不出现发布相关操作。

## 页面 1：项目首页

### 页面目标

让用户一眼知道当前项目处在哪个阶段，以及下一步该补什么。

### 页面区块

| 区块 | 内容 | 操作 |
| --- | --- | --- |
| 项目状态 | 项目名、当前状态、资料完整度 | 进入资料页 |
| 定位摘要 | 当前一句话定位、目标用户、业务场景 | 进入定位页 |
| P0 进度 | 资料、定位、选题、对标、草稿、审核 | 进入对应页面 |
| 风险提醒 | 资料不足、案例不足、标题超长、搬运风险 | 查看问题 |
| 下一步建议 | 当前最应该完成的一步 | 继续 |

### 状态样例

| 状态 | 页面呈现 | 主操作 |
| --- | --- | --- |
| 资料待补充 | 显示缺少服务对象、业务场景、案例 | 补充资料 |
| 可生成定位 | 显示资料已通过基础检查 | 生成定位 |
| 定位待确认 | 显示定位摘要和待确认提醒 | 确认定位 |
| 可生成选题 | 显示定位已确认 | 生成选题 |
| 草稿待审核 | 显示草稿风险清单 | 进入审核 |

### 空状态

当没有项目时：

- 显示创建项目入口；
- 说明需要先填写业务资料；
- 不显示内容生成入口。

### 失败状态

当项目方向不匹配时：

- 提示该产品不支持爆款搬运、洗稿和自动获客；
- 引导用户补充产品、服务或专业能力；
- 不生成定位和内容。

## 页面 2：业务资料

### 页面目标

收集足够支撑定位诊断的资料。

页面不追求字段多，而是追求字段能判断。

P0 首屏只展示 8 个必填判断字段，其余信息按需追问。

### 页面区块

#### 1. 必填判断字段

字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 当前角色 | 单选 | 是 | AI 工具顾问、工作流顾问、自动化顾问等 |
| 服务对象 | 文本 | 是 | 具体到一类人或团队 |
| 核心业务场景 | 文本 | 是 | 用户真实工作里的问题 |
| 服务或产品 | 单选/文本 | 是 | 咨询、课程、工作流搭建、训练营等 |
| 可感知结果 | 文本 | 是 | 用户能获得什么变化，不写夸大承诺 |
| 主要 AI 工作流 | 文本 | 是 | 例如选题、脚本、资料整理、自动化 |
| 真实案例或经验 | 文本 | 是 | 可用最小结构填写，没有则标记案例不足 |
| 内容边界 | 文本 | 是 | 不想做什么、不能承诺什么、不能公开什么 |

#### 2. 补充信息

默认折叠，系统需要时再追问：

| 字段 | 类型 | 触发时机 | 说明 |
| --- | --- | --- | --- |
| 常用工具 | 多选/文本 | 工作流不清楚时 | 工具不是定位核心 |
| 方法论 | 文本 | 选题缺少判断时 | 用户自己的判断框架 |
| 价格区间 | 文本 | 转化路径不清楚时 | 判断信任门槛 |
| 当前账号状态 | 单选/文本 | 定位偏差不清楚时 | 判断从哪里调整 |
| 用户常问问题 | 文本 | 选题不具体时 | 生成痛点型选题 |
| 失败或踩坑经验 | 文本 | 内容太像教程时 | 生成避坑型选题 |
| 对标账号 | 文本 | 需要结构参考时 | 只做结构拆解 |

#### 3. 风险边界

风险边界在生成内容前必须确认：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 不想变成的账号 | 多选/文本 | 是 | 工具清单号、提示词合集号等 |
| 不能承诺什么 | 文本 | 是 | 爆款、成交、自动赚钱等 |
| 不能公开什么 | 文本 | 是 | 客户信息、内部流程等 |

### 主操作

- 保存资料；
- 检查资料完整性；
- 生成补充问题；
- 进入定位诊断。

### 状态规则

| 条件 | 状态 | 处理 |
| --- | --- | --- |
| 缺服务对象 | 资料不足 | 要求补充 |
| 缺业务场景 | 资料不足 | 要求补充 |
| 只有工具名 | 资料不足 | 要求补工作流 |
| 没有案例但有经验 | 可初步定位 | 不生成强案例内容 |
| 有案例和边界 | 可完整生成 | 进入定位诊断 |
| 有隐私风险 | 存在风险需修改 | 要求先脱敏 |

### 空状态

新项目进入时：

- 显示最小必填字段；
- 先收 8 个必填判断字段；
- 不让用户先贴爆款链接。
- 补充字段默认折叠。

## 页面 3：定位诊断

### 页面目标

把用户从“我会很多工具”收敛成“我能帮哪类人解决哪类业务问题”。

### 页面区块

| 区块 | 内容 | 操作 |
| --- | --- | --- |
| 资料检查结果 | 哪些通过、哪些不足 | 返回补资料 |
| 一句话定位 | 我帮助谁，用什么工作流，解决什么问题 | 编辑 |
| 目标用户判断 | 核心用户、次级用户、暂不服务用户 | 编辑 |
| 业务场景 | 3 个核心业务场景 | 编辑 |
| 内容主张 | 不是……而是…… | 编辑 |
| 专业能力证明 | 案例、方法论、判断能力 | 补充案例 |
| 差异化 | 相比工具号、教程号、热点号的区别 | 编辑 |
| 不建议方向 | 不做什么账号 | 确认 |

### 主操作

- 重新生成定位；
- 编辑定位；
- 标记不准确；
- 确认定位；
- 返回补资料。

### 状态规则

| 条件 | 状态 | 处理 |
| --- | --- | --- |
| 定位只写工具分享 | 定位不成立 | 重写 |
| 服务对象太泛 | 需要补充资料 | 返回资料页 |
| 有定位但案例不足 | 定位待确认 | 提示案例风险 |
| 用户确认定位 | 定位已确认 | 进入选题 |

### 页面文案原则

- 不夸用户“很专业”；
- 直接指出不建议方向；
- 用“不是……而是……”帮助用户理解取舍；
- 每个结论都能追溯到用户资料。

## 页面 4：内容主线和选题库

### 页面目标

把定位诊断转成能持续发布的内容系统。

### 页面区块

#### 1. 内容主线区

每条主线展示：

| 字段 | 说明 |
| --- | --- |
| 主线名称 | 例如业务场景拆解、工作流复盘 |
| 解决的用户问题 | 对应目标用户的真实痛点 |
| 对应服务价值 | 这条内容如何建立信任或转化 |
| 可引用素材 | 案例、方法论、踩坑经验 |
| 适合内容类型 | 场景型、案例型、避坑型等 |
| 转化目标 | 咨询、资料领取、服务说明等 |

#### 2. 选题库区

每个选题展示：

| 字段 | 说明 |
| --- | --- |
| 选题标题 | 小红书标题默认不超过 20 个字 |
| 类型 | 场景型、案例型、工作流拆解型等 |
| 目标用户痛点 | 该选题解决谁的问题 |
| 业务场景 | 必须具体 |
| 可引用案例/经验 | 没有则标记风险 |
| 转化目标 | 信任、咨询、领取资料等 |
| 风险边界 | 不能承诺或不能公开的内容 |

### 筛选和操作

筛选：

- 按内容主线；
- 按选题类型；
- 按业务场景；
- 按转化目标；
- 按风险状态。

操作：

- 生成选题；
- 重新生成某条主线选题；
- 收藏选题；
- 删除选题；
- 标记风险；
- 选择选题生成草稿。

### 状态规则

| 条件 | 状态 | 处理 |
| --- | --- | --- |
| 定位未确认 | 不可生成选题 | 返回定位页 |
| 选题只讲工具 | 选题需修改 | 补业务场景 |
| 标题超过 20 个字 | 选题需修改 | 重写标题 |
| 有业务场景和转化目标 | 可生成内容草稿 | 进入草稿页 |

### 空状态

定位未确认时：

- 显示“先确认定位”；
- 不显示生成选题按钮；
- 提供返回定位入口。

## 页面 5：对标结构参考

### 页面目标

让用户学习对标账号的结构，而不是复制标题和正文。

### 页面区块

| 区块 | 内容 | 操作 |
| --- | --- | --- |
| 对标输入 | 账号名、内容链接或人工描述 | 添加 |
| 喜欢的部分 | 标题结构、开头方式、案例组织等 | 标记 |
| 不想学习的部分 | 夸大、洗稿、工具堆砌等 | 标记 |
| 可借鉴点 | 结构层面的借鉴 | 转成结构 |
| 不可照搬点 | 标题、正文、语气、案例等 | 确认 |
| 适配建议 | 当前用户应该怎么改 | 生成选题 |

### 主操作

- 添加对标；
- 拆解结构；
- 标记不可照搬；
- 转成适合当前定位的选题；
- 返回选题库。

### 状态规则

| 条件 | 状态 | 处理 |
| --- | --- | --- |
| 对标和当前定位不匹配 | 对标不适合 | 不生成选题 |
| 用户要求照搬标题 | 存在搬运风险 | 拦截 |
| 已拆出结构且可适配 | 对标可参考 | 转成选题 |

### 风险提示

页面必须固定显示：

- 对标只用于结构参考；
- 不复制标题；
- 不复制正文；
- 不做洗稿版本；
- 所有建议必须回到自己的业务场景和案例。

## 页面 6：单篇内容草稿

### 页面目标

把一个合格选题转成可编辑的小红书笔记和短视频脚本。

### 页面区块

#### 1. 选题信息

显示：

- 选题标题；
- 所属内容主线；
- 业务场景；
- 转化目标；
- 风险边界。

#### 2. 小红书笔记草稿

字段：

| 字段 | 要求 |
| --- | --- |
| 标题 | 不超过 20 个字 |
| 封面主标题 | 表达具体问题或判断 |
| 封面副标题 | 补充业务场景 |
| 开头 | 先讲场景，不先讲工具 |
| 正文结构 | 问题、判断、流程、案例、边界 |
| 评论引导 | 自然，不硬推 |
| 私信引导 | 不做自动私信，不承诺结果 |

#### 3. 短视频脚本

字段：

| 字段 | 要求 |
| --- | --- |
| 开场钩子 | 指向业务场景 |
| 场景描述 | 用户真实工作问题 |
| 常见误区 | 不夸大 |
| 工作流拆解 | 说明 AI 在哪里介入 |
| 人工判断位置 | 明确哪些不能自动化 |
| 案例或经验 | 必须真实 |
| 结尾引导 | 自然承接服务 |

#### 4. 人工审核区

审核项：

- 标题是否超过 20 个字；
- 是否绑定业务场景；
- 是否体现真实案例或经验；
- 是否过度工具化；
- 是否有夸大承诺；
- 是否有搬运或洗稿风险；
- 是否暴露客户隐私；
- 是否保留人工编辑空间。

### 主操作

- 生成草稿；
- 编辑草稿；
- 重新生成某一段；
- 进入人工审核；
- 返回选题库；
- 返回补资料。

### 状态规则

| 条件 | 状态 | 处理 |
| --- | --- | --- |
| 选题未合格 | 不可生成草稿 | 返回选题库 |
| 缺案例但写成案例复盘 | 草稿需修改 | 改成方法论或经验型 |
| 标题超长 | 草稿需修改 | 重写标题 |
| 出现夸大承诺 | 需要重写 | 删除风险表达 |
| 审核通过 | 审核通过 | 可人工交付 |

## 全局状态样例

### 资料不足状态

页面表现：

- 当前状态显示为 `资料不足`；
- 明确列出缺失字段；
- 提供 3-5 个补充问题；
- 不出现生成正文入口。

### 案例不足状态

页面表现：

- 当前状态显示为 `案例不足`；
- 允许生成初步定位；
- 选题类型中弱化案例型；
- 草稿生成时禁止写成客户结果复盘。

### 搬运风险状态

页面表现：

- 当前状态显示为 `存在搬运风险`；
- 标记风险来源；
- 说明只能学习结构；
- 不生成改写稿。

### 审核不通过状态

页面表现：

- 当前状态显示为 `需要重写`；
- 显示未通过审核项；
- 标记应该退回的页面；
- 不出现发布入口。

## 关键跳转

### 正常路径

| 当前页面 | 条件 | 下一页 |
| --- | --- | --- |
| 首页 | 点击补充资料 | 业务资料 |
| 业务资料 | 资料检查通过 | 定位诊断 |
| 定位诊断 | 定位已确认 | 内容主线和选题库 |
| 选题库 | 选择合格选题 | 单篇内容草稿 |
| 对标结构参考 | 生成适配选题 | 内容主线和选题库 |
| 单篇内容草稿 | 审核通过 | 停在草稿页 |

### 退回路径

| 当前页面 | 问题 | 退回页 |
| --- | --- | --- |
| 定位诊断 | 缺业务场景 | 业务资料 |
| 选题库 | 定位不成立 | 定位诊断 |
| 单篇内容草稿 | 选题不合格 | 内容主线和选题库 |
| 单篇内容草稿 | 案例不真实 | 业务资料 |
| 对标结构参考 | 搬运风险 | 对标结构参考 |

## P0 验收标准

### 信息架构

- 6 个页面能覆盖 P0 主路径；
- 每个页面都有明确输入、输出和主操作；
- 每个页面都能对应至少一个状态；
- 用户始终知道下一步应该做什么；
- 任何页面都不能绕过定位诊断。

### 生成边界

- 不允许资料不足时生成正文；
- 不允许定位未确认时生成选题；
- 不允许选题不合格时生成草稿；
- 不允许草稿未审核时进入发布；
- 不允许对标内容生成洗稿版本。

### 内容质量

- 小红书标题默认不超过 20 个字；
- 内容必须绑定业务场景；
- 内容必须体现 AI 工作流能力；
- 内容必须保留人工编辑空间；
- 内容不得承诺爆款、涨粉、成交或自动获客。

### 阶段边界

- 当前不做视觉设计；
- 当前不做前端开发；
- 当前不做服务端开发；
- 当前不做自动发布；
- 当前不做自动私信；
- 当前不展开首批用户执行。

## 下一步

1. 基于 `22_AI效率工具顾问P0模拟用户样例与路径跑通.md` 的结果，压缩业务资料页字段；
2. 将资料页字段拆成“必填判断字段”和“补充字段”；
3. 再决定是否进入可视化低保真原型；
4. 当前仍不进入开发，也不展开首批用户执行。
